1. Se creo la carpeta y solo se dio el comando npm install angular, el construye todas las dependencias la carpeta node_modules y el archivo package.json dentro de ella.
2. Lo anterior si se quiere manejas y descargar dependencias con npm, si se desea por ejemplo con bower entonces npm install bowwer.
3. Y con el mismo bower instalar angular asi: bower install angular